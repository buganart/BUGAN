# Create a survey using the typeform API

## Dependencies

    pip install typeform


## Usage

Put the typeform token into `tokeform_token.txt`.

    python upload_images.py /path/to/img/dir

    python create_survey.py
